/**
 * Subcollection of `echoEvaluationDrafts/{evaluationId}` for real-time dialogue messages.
 *
 * Document fields (client-written): `authorUid`, `authorDisplayName` (string, optional), `text`, `createdAt` (timestamp).
 */
export const echoEvaluationDraftThreadMessagesSubcollectionId = "threadMessages";
